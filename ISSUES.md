# 问题 & 目标跟踪

> 使用中发现的问题与后续开发目标，分类记录。已完成的任务直接删除，不留历史。

---

## 🐛 待修复问题

- [ ] **聊天对象头像支持更换** — 目前联系人头像仅用纯色圆形代替，需要支持用户自定义更换头像图片。
- [ ] **聊天列表预览文字显示最后一条消息** — 当前 `ChatListActivity` 中联系人的 `preview` 是写死的固定文本，应该动态显示该联系人的最后一条聊天记录。
- [ ] **主动消息偶发截断** — LLM 生成主动消息时偶尔会被截断，可能是 `maxTokens` 设置偏小或 API 提前终止，待排查。
- [ ] **发消息频率需继续调参** — 注意力机制已接入，但 PRESENT/ABSENT 下触发频率仍需根据实际感受继续调整。

---

## 🎯 后续开发目标

- [ ] **去掉正式版 `[主动关心]` 前缀** — 测试标记，正式版删除。
- [ ] **注意力节律轮询 — Android 端** — web-shell 已实现，Android 的 TriggerScheduler 仍为固定间隔，需同步。
- [ ] **Companion 长期记忆深化** — web-shell 已有基础 LLM 记忆更新，需进一步结构化记忆提取和检索。
- [ ] **实现 plan 中剩余 Phase** — InteractionState 的 ABSENT 三种策略细化、用户消息驱动源的情感分析 LLM 化等。
